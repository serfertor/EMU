# Быстрый старт - Эмулятор Процессора

## 🚀 Быстрый запуск

### Windows
```bash
python processor_emulator.py
```

### Linux/macOS
```bash
python3 processor_emulator.py
```

Или используйте скрипт:
```bash
chmod +x install_and_run.sh
./install_and_run.sh
```

---

## 📝 Структура файлов

```
├── processor_emulator.py      # Основной файл - эмулятор с GUI
├── README.md                  # Полная документация
├── examples_and_tests.py      # Примеры программ и тесты
├── install_and_run.sh         # Скрипт установки (Linux/macOS)
└── QUICKSTART.md              # Этот файл
```

---

## 💻 Системные требования

- **Python**: 3.8 или выше
- **ОС**: Windows, Linux, macOS
- **Библиотеки**: tkinter (встроена в Python)

### Установка зависимостей

**Debian/Ubuntu:**
```bash
sudo apt-get install python3-tk
```

**Fedora/RHEL:**
```bash
sudo dnf install python3-tkinter
```

**macOS:**
```bash
# Обычно уже установлена, или переустановите Python
python3 -m tkinter
```

**Windows:**
- Выберите опцию "tcl/tk and IDLE" при установке Python

---

## 🎯 Первые шаги

### 1. Загрузить готовый пример
1. Нажмите кнопку **"Задача 1: Сумма"** или **"Задача 2: Свертка"**
2. Программа загрузится автоматически

### 2. Пошаговое выполнение
1. Нажмите кнопку **"Загрузить"** для загрузки программы
2. Нажимайте **"Шаг"** для выполнения по одной команде
3. Смотрите изменения в регистрах и памяти

### 3. Автоматическое выполнение
1. Нажмите **"Загрузить"**
2. Нажмите **"Запуск"** для выполнения всей программы до конца

---

## 📚 Примеры программ

### Пример 1: Простое сложение
```asm
mov eax 5
add eax 3
```
**Результат**: EAX = 8

### Пример 2: Работа с памятью
```asm
mov [0] 42
mov eax [0]
add eax 8
```
**Результат**: EAX = 50 ([0] = 42)

### Пример 3: Простой цикл
```asm
mov eax 0
mov ecx 5
add eax ecx
sub ecx 1
jnz 2
```
**Результат**: EAX = 5 + 4 + 3 + 2 + 1 = 15

---

## 🎮 Интерфейс пользователя

### Секция "Регистры и Флаги"
- **EAX, EBX, ECX, EDX** - 4 регистра общего назначения
- **Z** - флаг нуля (1 если результат = 0)
- **S** - флаг знака (1 если результат < 0)

### Секция "Управление"
- **PC** - программный счётчик (текущая инструкция)
- **IR** - регистр команд (текущая команда)
- **Bin** - двоичное представление инструкции

### Секция "Память данных"
- Адрес и значение всех заполненных ячеек памяти
- Обновляется в реальном времени

### Секция "Консоль выполнения"
- Лог всех выполненных команд
- Сообщения об ошибках
- Статус выполнения

---

## 🔧 Команды ассемблера

### Передача данных
```asm
MOV eax 5           # Загрузить 5 в EAX
MOV [0] 10          # Загрузить 10 в памяти по адресу 0
MOV eax [0]         # Загрузить значение из [0] в EAX
```

### Арифметика
```asm
ADD eax ebx         # EAX += EBX
SUB eax 5           # EAX -= 5
MUL eax ecx         # EAX *= ECX
DIV eax ebx         # EAX /= EBX (целое деление)
```

### Логические операции
```asm
AND eax 15          # EAX &= 15
OR eax 1            # EAX |= 1
XOR eax 255         # EAX ^= 255
NOT eax             # EAX = ~EAX
```

### Управление потоком
```asm
JMP 5               # Безусловный переход на строку 5
JZ 10               # Переход если Z=1 (результат = 0)
JNZ 3               # Переход если Z=0 (результат != 0)
JS 7                # Переход если S=1 (результат < 0)
JNS 4               # Переход если S=0 (результат >= 0)
```

### Специальные
```asm
INC eax             # EAX++
CMP eax ebx         # Сравнить EAX и EBX (обновляет флаги)
```

---

## 🚨 Типичные ошибки и решения

### "Ошибка: Неизвестная команда"
- Проверьте опечатки в имени команды
- Команды чувствительны к регистру: `add` ✓, `ADD` ✗

### "Ошибка выполнения"
- Проверьте корректность операндов
- Не все команды поддерживают все типы адресации

### Программа зависает
- Возможно бесконечный цикл
- Проверьте условие выхода из цикла (JNZ должен выполниться)

### Неправильный результат
- Помните, что флаги Z и S обновляются только после ADD, SUB, CMP
- Проверьте порядок операций и адреса памяти

---

## 📊 Система команд в деталях

### Коды операций (4 бита)
```
0000 = MOV    0100 = DIV    1000 = JS     1100 = XOR
0001 = ADD    0101 = JMP    1001 = JNS    1101 = NOT
0010 = SUB    0110 = JZ     1010 = AND    1110 = CMP
0011 = MUL    0111 = JNZ    1011 = OR     1111 = INC
```

### Типы адресации (2 бита)
```
00 = Регистровая      (eax, ebx, ecx, edx)
01 = Непосредственная (123, -45)
10 = Косвенно-регистровая ([eax], [ecx])
11 = Прямая           ([0], [100])
```

---

## 🧮 Практические примеры

### Вычисление факториала 5!
```asm
mov [0] 5
mov ecx [0]
mov eax 1
mul eax ecx
sub ecx 1
jnz 3
```
**Результат**: EAX = 120

### Поиск максимума в массиве
```asm
mov eax [1]
mov ecx 2
mov ebx 5
cmp eax [ecx]
jns 5
mov eax [ecx]
inc ecx
cmp ecx ebx
jnz 3
```

### Проверка чётности числа
```asm
mov eax 42
and eax 1
xor eax 1
```
**Результат**: EAX = 1 (чётное число)

---

## 🔍 Отладка

### Включение консоли
- Консоль автоматически показывает все выполненные команды
- Каждая команда нумеруется по порядку выполнения

### Проверка состояния
1. Посмотрите значения регистров в реальном времени
2. Проверьте содержимое памяти
3. Смотрите значения флагов (Z, S)

### Шаг за шагом отладка
1. Загрузите программу
2. Нажимайте кнопку "Шаг" для пошагового выполнения
3. После каждого шага проверяйте состояние системы

---

## ✅ Checklist для первого запуска

- [ ] Установлена версия Python 3.8+
- [ ] Установлена библиотека tkinter
- [ ] Файл `processor_emulator.py` в текущей директории
- [ ] Эмулятор запустился без ошибок
- [ ] Загружена и выполнена задача 1
- [ ] Загружена и выполнена задача 2
- [ ] Написана и выполнена собственная программа

---

## 📞 Помощь и поддержка

### Проблемы с запуском
1. Проверьте версию Python: `python3 --version`
2. Проверьте tkinter: `python3 -c "import tkinter"`
3. Переустановите зависимости

### Проблемы с программой
1. Проверьте синтаксис в консоли
2. Используйте пошаговое выполнение для отладки
3. Смотрите примеры в файле `examples_and_tests.py`

### Расширение функциональности
- Отредактируйте `processor_emulator.py` для добавления новых команд
- Добавьте новые примеры в `examples_and_tests.py`

---

**Версия**: 1.0  
**Последнее обновление**: Декабрь 2025  
**Статус**: Готово к использованию
